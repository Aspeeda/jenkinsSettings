---

- view:
    name: QA
    view-type: list
    description: |
      QA projects
    filter-executors: true
    filter-queue: true
    job-name:
       - ui-autotests
       - api-autotests
       - mobile-autotests
       - runner-autotests
       - deploy-jobs
    columns:
      - status
      - build-button
      - job
      - weather